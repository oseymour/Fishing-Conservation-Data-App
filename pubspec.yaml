name: conservation_data
description: App for anglers to submit basic conservation data to the DNR.
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  date_field: ^6.0.0
  sprintf: ^7.0.0
  sqflite: ^2.4.2
  path: ^1.9.1
  provider: ^6.1.5
  uuid: ^4.5.1
  logger: ^2.6.1
  grouped_list: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
